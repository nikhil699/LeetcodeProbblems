// Problem Statement
// Find the Kth Last Element in a Singly Linked List
// You are given the head of a singly linked list and an integer k.
// Return the value of the node that is the kth element from the end of the list.
// Rules
// The linked list contains n nodes.
// 1 ≤ k ≤ n
// If k = 1, return the last element.
// If k = n, return the first element.
// If k > n, return -1 (or throw an exception, depending on requirement).
// Input Format
// A singly linked list
// An integer k
// Example 1
// Input:Linked List: 1 → 2 → 3 → 4 → 5
// k = 2
// Output:4
// Explanation:
// The 2nd last element is 4.
// Example 2
// Input:Linked List: 10 → 20 → 30 → 40
// k = 1
// Output:40
// Example 3
// Input:Linked List: 7 → 14 → 21
// k = 3
// Output:7
// Example 4 (Edge Case)
// Input:Linked List: 5 → 9
// k = 5
// Output:-1
// Constraints
// 1 ≤ n ≤ 10^5
// Values can be any integer
// Time complexity target: O(n)
// Space complexity target: O(1)

// Approach --- time Complexity - 0(n) where n is the number of node we have to iterate.
/// how to optimize it more.
node -> val, next (head)null[]-[],[]-[]-tail(null)

public class linkedList {
    
}
